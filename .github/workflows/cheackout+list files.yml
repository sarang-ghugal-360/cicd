name: Checkout and list files
on:

   push:
     branches:
      -main

jobs:

   checkout-and-list:
     runs-on: ubuntu-latest  
        steps:
        - name: Checkout code
        uses: actions/checkout@v2
         - name: List files
          run: ls -la
